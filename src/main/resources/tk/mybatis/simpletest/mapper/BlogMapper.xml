<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="tk.mybatis.simpletest.mapper.BlogMapper">

    <resultMap id="userMap" type="tk.mybatis.simpletest.model.BlogUser">
        <id property="userid" column="userid"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="userinfo" column="userinfo"/>
    </resultMap>

    <resultMap id="BlogDtoMap" type="tk.mybatis.simpletest.model.BlogDto">
        <constructor>
            <idArg column="id" javaType="String"/>
        </constructor>
        <result property="title" column="title"/>
        <result property="url" column="url"/>
        <result property="userid" column="userid"/>
        <association property="user" column="userid" fetchType="lazy" select="selectUser"/>
    </resultMap>

    <select id="selectById" resultMap="BlogDtoMap" parameterType="java.lang.String">
    select a.id,
       a.title,
       a.userid,
       a.url
      from blog a
     where id = #{id}
    </select>

    <select id="selectUser" resultMap="userMap">
        select userid,username,password,userinfo from blog_user where userid = #{userid};
    </select>

</mapper>